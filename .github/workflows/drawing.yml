name: blank

on:
  push:

jobs:
  potential:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        deposit: ["cheat", "bare", "come", "conclusion", "decrease"]
        suspect: ["principle", "liable", "emit", "generally"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar BN
